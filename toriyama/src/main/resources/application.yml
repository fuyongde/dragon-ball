server:
  port: 9000

spring:
  application:
    name: toriyama
  http:
    encoding:
      charset: UTF-8

eureka:
  instance:
    hostname: toriyama.dragon.com
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/